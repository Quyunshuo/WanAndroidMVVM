<resources>

    <!-- Base application theme. -->
    <style name="common_AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/common_theme</item>
        <item name="colorPrimaryDark">@color/common_theme</item>
        <item name="colorAccent">@color/common_theme</item>

        <item name="android:windowBackground">@color/common_background</item>
        <item name="android:navigationBarColor">@color/common_background</item>
    </style>

</resources>
